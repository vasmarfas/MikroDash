// GENERATED by cmd/tsgen — do not edit.
//
// The WebSocket payload types and the event map, read from the Go declarations
// that send them (`hub.Declare`, internal/hub/event.go). Every property, its
// type and its optionality come from a json tag. `go run ./cmd/tsgen -check`
// fails when this file is stale, and tools/verify.sh runs it.
//
// Optional (`?`) means the Go field carries `omitempty` and the key can be
// ABSENT. `| null` means the Go field is a pointer and the key is present
// carrying null. They are different things and the distinction is load-bearing:
// the dashboard's count setters test `!== undefined`, so an absent key renders an
// em dash while an explicit null renders the string "null". Arrays are never
// null: Go does not send one, and TestNoPayloadSendsANullArray holds it to that.

export interface AlertRow {
  id: number;
  routerId: string | null;
  alertType: string;
  label: string;
  routerName: string | null;
  subject: string | null;
  detail: string | null;
  firedAt: number;
  resolvedAt: number | null;
  acknowledgedAt: number | null;
  acknowledgedBy: string | null;
}

export interface Settings {
  enabled: boolean;
  schedule: string;
  time: string;
  timezone: string;
  keepCount: number;
  keepDays: number;
}

export interface BackupSummary {
  runs: number;
  stored: number;
  bytes: number;
  lastAt: number;
  lastOutcome: string | null;
}

export interface BackupRow {
  id: number;
  takenAt: number;
  outcome: string;
  source: string;
  actor: string | null;
  stem: string | null;
  pruned: boolean;
  bytes: number;
  osVersion: string | null;
  model: string | null;
  serial: string | null;
  ms: number;
  error: string | null;
}

export interface StatePayload {
  routerId: string;
  label: string;
  settings: Settings;
  summary: BackupSummary;
  running: boolean;
  permitted: boolean;
  rows: BackupRow[];
}

export interface BandwidthDevice {
  srcIp: string;
  dstIp: string;
  rxMbps: number;
  txMbps: number;
  totalMbps: number;
  proto: string;
  iface: string;
  name: string;
  mac: string;
  country: string;
  city: string;
  org: string | null;
  cat: string | null;
  isLan: boolean;
  isIpv6: boolean;
}

export interface BandwidthPayload {
  ts: number;
  devices: BandwidthDevice[];
  pollMs: number;
}

export interface Bridge {
  id: string;
  name: string;
  protocolMode: string;
  vlanFiltering: boolean;
  igmpSnooping: boolean;
  dhcpSnooping: boolean;
  fastForward: boolean;
  priority: string;
  ageingTime: string;
  macAddress: string;
  mtu: number | null;
  running: boolean;
  disabled: boolean;
  comment: string;
  portCount: number;
  rxMbps: number | null;
  txMbps: number | null;
}

export interface BridgePort {
  id: string;
  bridge: string;
  interface: string;
  pvid: number | null;
  role: string;
  edge: string;
  learn: string;
  horizon: string;
  pathCost: number | null;
  frameTypes: string;
  disabled: boolean;
  inactive: boolean;
  dynamic: boolean;
}

export interface BridgeHost {
  mac: string;
  onInterface: string;
  bridge: string;
  vid: number | null;
  dynamic: boolean;
  local: boolean;
  external: boolean;
  age: string;
}

export interface BridgesPayload {
  ts: number;
  pollMs: number;
  bridges: Bridge[];
  ports: BridgePort[];
  hosts: BridgeHost[];
  hostTotal: number;
  hostCap: number;
  ratesAvailable: boolean;
  available: boolean;
  hostsAvailable: boolean;
}

export interface CapsManager {
  enabled: boolean;
  interfaces: string[];
  caCertificate: string;
  certificate: string;
  requirePeerCertificate: boolean;
  upgradePolicy: string;
  packagePath: string;
}

export interface CapsCapMode {
  enabled: boolean;
  discoveryInterfaces: string[];
  capsManAddresses: string[];
  currentAddress: string;
  currentIdentity: string;
  certificate: string;
  slavesDatapath: string;
}

export interface CapsRadio {
  radioMac: string;
  interface: string;
  disabled: boolean;
}

export interface CapsClient {
  mac: string;
  interface: string;
  ssid: string;
  signal: number | null;
  uptime: string;
}

export interface Cap {
  identity: string;
  address: string;
  boardName: string;
  serial: string;
  version: string;
  baseMac: string;
  commonName: string;
  state: string;
  connectedTime: string;
  uptime: string;
  radios: CapsRadio[];
  clients: CapsClient[];
  clientCount: number;
  legacy: boolean;
}

export interface CapsProvisioning {
  id: string;
  identity: string;
  supportedBands: string[];
  action: string;
  masterConfiguration: string;
  slaveConfigurations: string[];
  nameFormat: string;
  radioMac: string;
  identityRegexp: string;
  comment: string;
  disabled: boolean;
  legacy: boolean;
}

export interface CapsTotals {
  caps: number;
  capsOk: number;
  radios: number;
  clients: number;
  clientsOnCaps: number;
  clientsLocal: number;
}

export interface CapsConfigProfile {
  id: string;
  name: string;
  ssid: string;
  mode: string;
  country: string;
  hideSsid: boolean;
  security: string;
  channel: string;
  datapath: string;
  manager: string;
  comment: string;
  disabled: boolean;
  legacy: boolean;
}

export interface CapsSecurityProfile {
  id: string;
  name: string;
  authTypes: string;
  wps: string;
  ft: boolean;
  comment: string;
  disabled: boolean;
  legacy: boolean;
}

export interface CapsChannelProfile {
  id: string;
  name: string;
  band: string;
  frequency: string;
  width: string;
  secondaryFrequency: string;
  skipDfsChannels: string;
  comment: string;
  disabled: boolean;
  legacy: boolean;
}

export interface CapsDatapathProfile {
  id: string;
  name: string;
  bridge: string;
  vlanId: string;
  clientIsolation: boolean;
  localForwarding: boolean;
  trafficProcessing: string;
  comment: string;
  disabled: boolean;
  legacy: boolean;
}

export interface CapsProfiles {
  configuration: CapsConfigProfile[];
  security: CapsSecurityProfile[];
  channel: CapsChannelProfile[];
  datapath: CapsDatapathProfile[];
}

export interface CapsmanPayload {
  ts: number;
  pollMs: number;
  role: string;
  manager: CapsManager;
  cap: CapsCapMode;
  caps: Cap[];
  provisioning: CapsProvisioning[];
  localRadios: CapsRadio[];
  totals: CapsTotals;
  profiles: CapsProfiles;
  available: boolean;
  legacyAvailable: boolean;
  legacyManager: boolean;
}

export interface ConnProtoCounts {
  tcp: number;
  udp: number;
  icmp: number;
  other: number;
}

export interface ConnSource {
  ip: string;
  name: string;
  mac: string;
  count: number;
}

export interface ConnCountryProto {
  tcp: number;
  udp: number;
  other: number;
}

export interface ConnDestination {
  key: string;
  count: number;
  country: string;
  city: string;
  proto: ConnCountryProto;
  org: string | null;
  cat: string | null;
}

export interface ConnOrgCount {
  org: string;
  count: number;
  cat: string | null;
}

export interface ConnCountry {
  cc: string;
  city: string;
  count: number;
  proto: ConnCountryProto;
  orgs: ConnOrgCount[];
}

export interface ConnPort {
  port: string;
  count: number;
}

export interface ConnDestEntry {
  key: string;
  count: number;
  country: string;
  city: string;
  org: string | null;
  cat: string | null;
}

export interface ConnsPayload {
  ts?: number;
  total: number;
  processed: number;
  processingCapped: boolean;
  newSinceLast?: number;
  protoCounts: ConnProtoCounts;
  topSources: ConnSource[];
  topDestinations: ConnDestination[];
  topCountries: ConnCountry[];
  topPorts: ConnPort[];
  countryDests: Record<string, ConnDestEntry[]> | null;
  countryPorts: Record<string, ConnPort[]> | null;
  sourceDests: Record<string, ConnDestEntry[]> | null;
  sourcePorts: Record<string, ConnPort[]> | null;
  pollMs: number;
}

export interface MenuLoad {
  menu: string;
  streamed: boolean;
}

export interface AcqLayer {
  commandsPerMin: number;
  inFlight: number;
  cap: number;
  channels: number;
  menus: number;
  streamed: number;
  polled: number;
  reads: MenuLoad[];
  more: number;
}

export interface DerLayer {
  payloadsPerMin: number;
}

export interface ViewLayer {
  running: number;
  gated: number;
  dormant: number;
  rooms: number;
  holds: string[];
}

export interface Diagnostics {
  ts: number;
  routerId: string;
  label: string;
  acquisition: AcqLayer;
  derivation: DerLayer;
  views: ViewLayer;
}

export interface DNSSettings {
  servers: string[];
  dynamicServers: string[];
  dohEnabled: boolean;
  dohUrl: string;
  dohVerifyCert: boolean;
  dohMaxServerConnections: number | null;
  dohMaxConcurrentQueries: number | null;
  dohTimeout: string;
  allowRemoteRequests: boolean;
  cacheSize: number | null;
  cacheUsed: number | null;
  cacheMaxTtl: string;
  maxUdpPacketSize: number | null;
  maxConcurrentQueries: number | null;
  queryServerTimeout: string;
  queryTotalTimeout: string;
  mdnsRepeatIfaces: string[];
  vrf: string;
}

export interface DNSStaticEntry {
  id: string;
  name: string;
  regexp: string;
  address: string;
  type: string;
  ttl: string;
  disabled: boolean;
  comment: string;
}

export interface DNSPayload {
  ts: number;
  pollMs: number;
  settings: DNSSettings;
  staticEntries: DNSStaticEntry[];
  available: boolean;
}

export interface FirewallRule {
  id: string;
  chain: string;
  action: string;
  comment: string;
  srcAddress: string;
  dstAddress: string;
  protocol: string;
  dstPort: string;
  inInterface: string;
  packets: number;
  bytes: number;
  deltaPackets: number;
  disabled: boolean;
  dynamic: boolean;
}

export interface FirewallPayload {
  ts: number;
  filter: FirewallRule[];
  nat: FirewallRule[];
  mangle: FirewallRule[];
  raw: FirewallRule[];
  filter6?: FirewallRule[];
  nat6?: FirewallRule[];
  mangle6?: FirewallRule[];
  raw6?: FirewallRule[];
  ipv6Disabled?: boolean | null;
  activeTable: string;
  pollMs: number;
}

export interface IfName {
  name: string;
  running: boolean;
  disabled: boolean;
}

export interface IfNamesPayload {
  ts: number;
  total: number;
  interfaces: IfName[];
}

export interface Interface {
  name: string;
  type: string;
  running: boolean;
  disabled: boolean;
  comment: string;
  macAddr: string;
  rxMbps: number;
  txMbps: number;
  ips: string[];
  rxBytes: number | null;
  txBytes: number | null;
  errors: number | null;
  drops: number | null;
  linkDowns: number | null;
  lastLinkUp: string;
  errorsDelta: number | null;
  dropsDelta: number | null;
  deltaWindowMs: number | null;
}

export interface IfStatusPayload {
  ts: number;
  routerId: string;
  interfaces: Interface[];
}

export interface Network {
  cidr: string;
  gateway: string;
  dns: string;
  leaseCount: number;
  poolSize: number;
}

export interface InternetIface {
  name: string;
  ip: string;
}

export interface LanPayload {
  ts: number;
  lanCidrs: string[];
  networks: Network[];
  wanIp: string;
  totalPoolSize: number;
  totalLeases: number;
  pollMs: number;
  internetIfaces: InternetIface[];
}

export interface Lease {
  ip: string;
  name: string;
  mac: string;
  hostName: string;
  comment: string;
  status: string;
  server: string;
  iface: string;
  vlanId: string;
  id: string;
  dynamic: boolean;
}

export interface LeaseServer {
  name: string;
  iface: string;
  vlanId: string;
  count: number;
}

export interface LeasesPayload {
  ts: number;
  leases: Lease[];
  servers: LeaseServer[];
}

export interface LogEntry {
  ts: number;
  time: string;
  topics: string;
  message: string;
  severity: string;
}

export interface NetwatchHost {
  id: string;
  host: string;
  type: string;
  status: string;
  name: string;
  comment: string;
}

export interface NetwatchPayload {
  hosts: NetwatchHost[];
  ts: number;
}

export interface Package {
  id: string;
  name: string;
  version: string;
  buildTime: string;
  size: number | null;
  scheduled: string;
  scheduledAction: string;
  disabled: boolean;
  onServer: boolean;
  state: string;
}

export interface Firmware {
  isRouterboard: boolean;
  boardName: string;
  model: string;
  serial: string;
  firmwareType: string;
  currentFirmware: string;
  upgradeFirmware: string;
  minimumFirmware: string;
  upgradeAvailable: boolean;
}

export interface Update {
  channel: string;
  installedVersion: string;
  latestVersion: string;
  status: string;
  updateAvailable: boolean;
}

export interface PackageCounts {
  total: number;
  installed: number;
  disabled: number;
  available: number;
  scheduled: number;
}

export interface PackagesPayload {
  ts: number;
  pollMs: number;
  packages: Package[];
  firmware: Firmware;
  update: Update;
  counts: PackageCounts;
  pendingReboot: boolean;
  available: boolean;
}

export interface PingPayload {
  target: string;
  rtt: number | null;
  loss: number | null;
  minRtt?: number | null;
  maxRtt?: number | null;
  permissionDenied?: boolean;
  ts: number;
  pollMs: number;
}

export interface PPPSession {
  id: string;
  name: string;
  service: string;
  address: string;
  callerId: string;
  uptime: string;
  encoding: string;
  sessionId: string;
  limitIn: number | null;
  limitOut: number | null;
  rx: number;
  tx: number;
  rxRate: number | null;
  txRate: number | null;
}

export interface PPPSecret {
  id: string;
  name: string;
  service: string;
  profile: string;
  localAddress: string;
  remoteAddress: string;
  callerId: string;
  routes: string;
  limitIn: number | null;
  limitOut: number | null;
  comment: string;
  disabled: boolean;
  connected: boolean;
}

export interface PPPProfile {
  id: string;
  name: string;
  localAddress: string;
  remoteAddress: string;
  rateLimit: string;
  onlyOne: string;
  encryption: string;
}

export interface PPPServer {
  serviceName: string;
  interface: string;
  maxSessions: string;
  auth: string;
  disabled: boolean;
}

export interface PPPPayload {
  ts: number;
  pollMs: number;
  sessions: PPPSession[];
  secrets: PPPSecret[];
  profiles: PPPProfile[];
  servers: PPPServer[];
  byService: Record<string, number> | null;
  totalRxRate: number | null;
  totalTxRate: number | null;
  available: boolean;
}

export interface Pair {
  up: number | null;
  down: number | null;
}

export interface IntPair {
  up: number | null;
  down: number | null;
}

export interface RatePair {
  up: number | null;
  down: number | null;
}

export interface SimpleQueue {
  id: string;
  order: number;
  name: string;
  target: string;
  parent: string;
  packetMarks: string;
  priority: string;
  queueType: string;
  limitAt: Pair;
  maxLimit: Pair;
  burstLimit: Pair;
  bytes: IntPair;
  packets: IntPair;
  dropped: IntPair;
  queuedBytes: IntPair;
  disabled: boolean;
  invalid: boolean;
  dynamic: boolean;
  comment: string;
  rateBps: RatePair;
  rateSource: string | null;
  rateWindowMs: number | null;
}

export interface TreeQueue {
  id: string;
  order: number;
  name: string;
  parent: string;
  packetMark: string;
  priority: string;
  queueType: string;
  limitAt: number | null;
  maxLimit: number | null;
  burstLimit: number | null;
  bytes: number | null;
  packets: number | null;
  dropped: number | null;
  queuedBytes: number | null;
  disabled: boolean;
  invalid: boolean;
  dynamic: boolean;
  comment: string;
  fasttrackBypassable: boolean;
  rateBps: number | null;
  rateSource: string | null;
  rateWindowMs: number | null;
}

export interface Fasttrack {
  state: string;
  count: number;
  scoped: boolean;
}

export interface QueuesPayload {
  ts: number;
  pollMs: number;
  simple: SimpleQueue[];
  tree: TreeQueue[];
  fasttrack: Fasttrack;
  stats: string;
  available: boolean;
  denied: boolean;
}

export interface RosUser {
  id: string;
  name: string;
  group: string;
  address: string;
  comment: string;
  disabled: boolean;
  expired: boolean;
  lastLogin: string;
  inactivityTimeout: string;
  inactivityPolicy: string;
  protected: boolean;
}

export interface RosGroup {
  id: string;
  name: string;
  granted: string[];
  denied: string[];
  skin: string;
  comment: string;
  protected: boolean;
  members: number;
}

export interface RosSession {
  id: string;
  name: string;
  address: string;
  via: string;
  group: string;
  when: string;
  radius: boolean;
  protected: boolean;
}

export interface RosSelf {
  names: string[];
  groups: string[];
  resolved: boolean;
  source: string | null;
}

export interface RosPasswordPolicy {
  minLength: number;
  minCategories: number;
}

export interface RosUsersPayload {
  ts: number;
  pollMs: number;
  users: RosUser[];
  groups: RosGroup[];
  sessions: RosSession[];
  self: RosSelf;
  passwordPolicy: RosPasswordPolicy;
  policies: string[];
  available: boolean;
  denied: boolean;
}

export interface RouterStatsRow {
  id: string;
  label: string;
  host: string;
  isActive: boolean;
  connected: boolean;
  known: boolean;
  lastError: string | null;
  openAlerts: number;
  cpu: number | null;
  uptime: string | null;
  memPct: number | null;
  hddPct: number | null;
  version: string | null;
  boardName: string | null;
  arch: string | null;
  serial: string | null;
  licenseLevel: string | null;
  rxMbps: number | null;
  txMbps: number | null;
  clients: number | null;
  siteIds: string[];
  siteNames: string[];
  siteId: string | null;
  siteName: string | null;
  geo: { lat: number; lon: number; source: string; label: string; accuracyKm?: number | null; wanIp?: string } | null;
}

export interface RouteCounts {
  total: number;
  connect: number;
  static: number;
  dynamic: number;
  bgp: number;
  ospf: number;
}

export interface Peer {
  key: string;
  peerType: string;
  name: string;
  description: string;
  remoteAddr: string;
  remoteAs: number;
  state: string;
  uptimeSec: number;
  prefixes: number;
  prefixHistory: number[];
  updatesSent: number;
  updatesRecv: number;
  lastError: string;
  holdTime: number;
  keepalive: number;
  flapping: boolean;
}

export interface Route {
  dst: string;
  gateway: string;
  distance: number;
  active: boolean;
  comment: string;
  type: string;
  protocol: string;
  family: string;
  id: string;
}

export interface PeerSummary {
  total: number;
  established: number;
  down: number;
}

export interface RoutingPayload {
  ts: number;
  pollMs: number;
  routeCounts: RouteCounts;
  peers: Peer[];
  routes: Route[];
  summary: PeerSummary;
}

export interface Site {
  id: string;
  name: string;
  description: string | null;
  lat: number | null;
  lon: number | null;
  place_name: string | null;
  place_region: string | null;
  place_cc: string | null;
  created_at: number;
}

export interface SystemPayload {
  ts: number;
  uptimeRaw: string;
  cpuLoad: number;
  memPct: number;
  usedMem: number;
  totalMem: number;
  hddPct: number;
  totalHdd: number;
  freeHdd: number;
  version: string;
  latestVersion: string;
  updateAvailable: boolean;
  updateStatus: string;
  updateChannel: string;
  boardName: string;
  cpuCount: number;
  cpuFreq: number;
  tempC: number | null;
  pollMs: number;
  arch: string | null;
  serial: string | null;
  licenseLevel: string | null;
}

export interface TalkerDevice {
  name: string;
  mac: string;
  tx_mbps: number;
  rx_mbps: number;
}

export interface TalkersPayload {
  ts: number;
  devices: TalkerDevice[];
  pollMs: number;
  available: boolean;
}

export interface TopoDiscovery {
  protocol: string[];
  mode: string;
  interfaceList: string;
  interval: string;
}

export interface TopoVlan {
  vid: number;
  name: string;
}

export interface TopoCore {
  key: string;
  kind: string;
  name: string;
  identity: string;
  mac: string;
  ip: string;
  ip6: string;
  type: string;
  typeSource: string;
  caps: string[];
  capsEnabled: string[];
  platform: string;
  board: string;
  version: string;
  softwareId: string;
  description: string;
  uptime: string;
  ageSec: number | null;
  via: string[];
  running: string[];
  ifaces: string[];
  remoteIface: string;
  ipv6: boolean;
  port: string;
  parent: string | null;
  gone: boolean;
  firstSeen: number;
  lastSeen: number;
  rtt: number | null;
  loss: number | null;
  pingTs: number | null;
  cpuLoad: number | null;
  memPct: number | null;
  status: string;
  clientCount: number;
}

export interface TopoNeighbor {
  key: string;
  kind: string;
  name: string;
  identity: string;
  mac: string;
  ip: string;
  ip6: string;
  type: string;
  typeSource: string;
  caps: string[];
  capsEnabled: string[];
  platform: string;
  board: string;
  version: string;
  softwareId: string;
  description: string;
  uptime: string;
  ageSec: number | null;
  via: string[];
  running: string[];
  ifaces: string[];
  remoteIface: string;
  ipv6: boolean;
  gone: boolean;
  firstSeen: number;
  lastSeen: number;
  rtt: number | null;
  loss: number | null;
  pingTs: number | null;
  status: string;
  port: string;
  parent: string | null;
  pinned: boolean;
  clientCount: number;
}

export interface TopoClient {
  key: string;
  kind: string;
  name: string;
  identity: string;
  mac: string;
  ip: string;
  ip6: string;
  type: string;
  typeSource: string;
  caps: string[];
  capsEnabled: string[];
  platform: string;
  board: string;
  version: string;
  softwareId: string;
  description: string;
  uptime: string;
  ageSec: number | null;
  via: string[];
  running: string[];
  ifaces: string[];
  remoteIface: string;
  ipv6: boolean;
  port: string;
  parent: string;
  attrib: string;
  vlans: number[];
  vlanNames: string[];
  ssid: string;
  signal: string;
  gone: boolean;
  firstSeen: number;
  lastSeen: number;
  rtt: number | null;
  loss: number | null;
  pingTs: number | null;
  status: string;
}

export interface TopoEdge {
  id: string;
  from: string;
  to: string;
  iface: string;
  viaPort: string;
  remoteIface: string;
  shared: boolean;
  inferred: boolean;
  pinned: boolean;
  client?: boolean;
  gone: boolean;
}

export interface TopologyPayload {
  ts: number;
  routerId: string;
  pollMs: number;
  discovery: TopoDiscovery | null;
  permissionDenied: boolean;
  pingDenied: boolean;
  neighborCount: number;
  pinsEnabled: boolean;
  vlans: TopoVlan[];
  clientCount: number;
  clientsTruncated: number;
  nodes: (TopoCore | TopoNeighbor | TopoClient)[];
  edges: TopoEdge[];
}

export interface TrafficPoint {
  ts: number;
  rx_mbps: number;
  tx_mbps: number;
}

export interface TrafficHistory {
  ifName: string;
  windowMinutes: number;
  points: TrafficPoint[];
}

export interface TrafficSample {
  ifName: string;
  ts: number;
  rx_mbps: number;
  tx_mbps: number;
  running: boolean;
  disabled: boolean;
}

export interface VlanInterface {
  id: string;
  name: string;
  parent: string;
  mtu: number | null;
  running: boolean;
  disabled: boolean;
  comment: string;
  rxMbps: number | null;
  txMbps: number | null;
}

export interface Vlan {
  vlanId: number;
  interfaces: VlanInterface[];
  tagged: string[];
  untagged: string[];
  bridges: string[];
  clients: number;
  rxMbps: number | null;
  txMbps: number | null;
  name: string;
}

export interface BridgeVlan {
  bridge: string;
  raw: string;
  ids: number[];
  ranges: number[][];
  truncated: boolean;
  tagged: string[];
  untagged: string[];
  currentTagged: string[];
  dynamic: boolean;
  disabled: boolean;
}

export interface VlanPort {
  bridge: string;
  interface: string;
  pvid: number | null;
  frameTypes: string;
  disabled: boolean;
}

export interface VlansPayload {
  ts: number;
  pollMs: number;
  vlans: Vlan[];
  bridgeVlans: BridgeVlan[];
  ports: VlanPort[];
  dynamicCount: number;
  ratesAvailable: boolean;
}

export interface Tunnel {
  id: string;
  publicKey: string;
  type: string;
  name: string;
  state: string;
  comment: string;
  lastHandshake: string;
  keepalive: string;
  endpoint: string;
  allowedIp: string;
  interface: string;
  rx: number;
  tx: number;
  rxRate: number;
  txRate: number;
}

export interface PppTunnel {
  type: string;
  name: string;
  service: string;
  address: string;
  callerId: string;
  uptime: string;
  rx: number;
  tx: number;
}

export interface IpsecTunnel {
  type: string;
  name: string;
  state: string;
  uptime: string;
  side: string;
  enc: string;
  auth: string;
}

export interface VPNPayload {
  ts: number;
  tunnels: Tunnel[];
  ppp: PppTunnel[];
  ipsec: IpsecTunnel[];
  pollMs: number;
}

export interface WanStatus {
  ifName: string;
  ts: number;
  running: boolean;
  disabled: boolean;
}

export interface WANDhcp {
  id: string;
  status: string;
  server: string;
  primaryDns: string;
  secondaryDns: string;
  expiresAfter: string;
  invalid: boolean;
}

export interface WAN {
  name: string;
  type: string;
  isTunnel: boolean;
  state: string;
  manual: boolean;
  since: string;
  running: boolean | null;
  address: string;
  isPublic: boolean | null;
  gateway: string;
  routeDistance: string;
  routeActive: boolean;
  hasDefaultRoute: boolean;
  rxMbps: number | null;
  txMbps: number | null;
  rxBytes: number | null;
  txBytes: number | null;
  dhcp: WANDhcp | null;
}

export interface WANCandidate {
  name: string;
  type: string;
  running: boolean;
}

export interface WANPayload {
  ts: number;
  pollMs: number;
  wans: WAN[];
  ratesAvailable: boolean;
  activeDefaultWan: string;
  publicIp: string;
  detectionEnabled: boolean;
  available: boolean;
  denied: boolean;
  uplinkSource: string;
  manualNames: string[];
  interfaces: WANCandidate[];
}

export interface WifiRadio {
  name: string;
  ap: string;
  defaultName: string;
  mac: string;
  band: string;
  bandRaw: string;
  frequency: string;
  channelWidth: string;
  country: string;
  disabled: boolean;
  running: boolean;
  capsManaged: boolean;
  readOnlyReason: string;
  profile: string;
}

export interface WifiInherits {
  ssid: string | null;
  security: string | null;
  channel: string | null;
}

export interface WifiNetwork {
  id: string;
  name: string;
  ssid: string;
  radio: string;
  master: string;
  isVirtual: boolean;
  band: string;
  bandRaw: string;
  security: string;
  authTypes: string;
  hidden: boolean;
  vlanId: string;
  bridge: string;
  disabled: boolean;
  running: boolean;
  clients: number;
  comment: string;
  capsManaged: boolean;
  profile: string;
  profileUsedBy: number;
  inherits: WifiInherits | null;
  readOnlyReason: string;
  editable: boolean;
  removable: boolean;
  resource: string;
  ap: string;
}

export interface WifiSecProfile {
  id: string;
  name: string;
  mode: string;
  authTypes: string;
  security: string;
  isDefault: boolean;
}

export interface WifiTotals {
  radios: number;
  networks: number;
  clients: number;
  capsManaged: number;
  readOnly: number;
}

export interface WifiPayload {
  ts: number;
  pollMs: number;
  stack: string;
  available: boolean;
  radios: WifiRadio[];
  networks: WifiNetwork[];
  secProfiles: WifiSecProfile[];
  totals: WifiTotals;
}

export interface WirelessClient {
  mac: string;
  signal: number;
  iface: string;
  txRate: string;
  band: string;
  standard: string;
  ip: string;
  rxRate: string;
  uptime: string;
  ssid: string;
  name: string;
  comment: string;
  source?: string;
}

export interface WirelessSSID {
  ssid: string;
  ifaces: string[];
  bands: string[];
  disabled: boolean;
  running: boolean;
  clients: number;
}

export interface WirelessPayload {
  ts: number;
  clients: WirelessClient[];
  mode: string;
  pollMs: number;
  capsmanAvailable: boolean;
  ssids: WirelessSSID[];
  ssidsManagedElsewhere: number;
}

export interface DiffLine {
  op: string;
  text: string;
  aLine?: number | null;
  bLine?: number | null;
}

export interface Hunk {
  aStart: number;
  bStart: number;
  aCount: number;
  bCount: number;
  lines: DiffLine[];
}

export interface PingPoint {
  ts: number;
  rtt: number | null;
  loss: number | null;
  permissionDenied?: boolean;
}

export interface ResFieldError {
  field: string;
  message: string;
}

export interface WifiscanRow {
  ch: number;
  chNum: number | null;
  chRaw: string | null;
  nets: number | null;
  load: number | null;
  nf: number | null;
  maxSig: number | null;
  minSig: number | null;
  primary: boolean;
  secondary: boolean;
}

// The `conn:update` payload, which is NOT ConnsPayload.
//
// internal/collect/connections.go sends it through `ConnsLight`, which deletes
// 4 keys (countryDests, countryPorts, sourceDests, sourcePorts). They travel separately, as conn:country-data and
// conn:source-data -- the keys are ABSENT here, not null.
export interface ConnsUpdate {
  ts?: number;
  total: number;
  processed: number;
  processingCapped: boolean;
  newSinceLast?: number;
  protoCounts: ConnProtoCounts;
  topSources: ConnSource[];
  topDestinations: ConnDestination[];
  topCountries: ConnCountry[];
  topPorts: ConnPort[];
  pollMs: number;
}

// Every event whose payload is a struct, and its type. web/src/socket.ts types
// each handler from this, so a page listening for an event gets exactly the
// payload Go declared for it.
export interface Events {
  'alert:acked': AlertRow;
  'backups:state': StatePayload;
  'bandwidth:update': BandwidthPayload;
  'bridges:update': BridgesPayload;
  'capsman:update': CapsmanPayload;
  'conn:update': ConnsUpdate;
  'diagnostics:update': Diagnostics;
  'dns:update': DNSPayload;
  'firewall:update': FirewallPayload;
  'ifstatus:names': IfNamesPayload;
  'ifstatus:update': IfStatusPayload;
  'lan:overview': LanPayload;
  'leases:list': LeasesPayload;
  'logs:history': LogEntry[];
  'logs:new': LogEntry;
  'netwatch:update': NetwatchPayload;
  'packages:update': PackagesPayload;
  'ping:update': PingPayload;
  'ppp:update': PPPPayload;
  'queues:update': QueuesPayload;
  'rosusers:update': RosUsersPayload;
  'routers:stats': RouterStatsRow[];
  'routing:update': RoutingPayload;
  'sites:update': Site[];
  'system:update': SystemPayload;
  'talkers:update': TalkersPayload;
  'topology:update': TopologyPayload;
  'traffic:history': TrafficHistory;
  'traffic:update': TrafficSample;
  'vlans:update': VlansPayload;
  'vpn:update': VPNPayload;
  'wan:status': WanStatus;
  'wan:update': WANPayload;
  'wifi:update': WifiPayload;
  'wireless:update': WirelessPayload;
}

// The events whose payload is a map, and so has no struct to generate from.
// web/src/events-hand.ts types exactly these — tsc fails if it misses one or
// types one that is not here.
export type HandEventName =
  | 'access:none'
  | 'access:revoked'
  | 'alerts:cleared-all'
  | 'alerts:open'
  | 'backups:diff'
  | 'backups:error'
  | 'backups:ran'
  | 'backups:restored'
  | 'backups:restoring'
  | 'backups:running'
  | 'collection:config'
  | 'collection:status'
  | 'conn:country-data'
  | 'conn:source-data'
  | 'lan:wan'
  | 'packages:applying'
  | 'packages:caps'
  | 'packages:error'
  | 'packages:notes'
  | 'packages:ok'
  | 'perms:changed'
  | 'ping:history'
  | 'queues:caps'
  | 'queues:error'
  | 'queues:ok'
  | 'res:error'
  | 'res:history'
  | 'res:new'
  | 'res:ok'
  | 'res:preview'
  | 'res:row'
  | 'res:schema'
  | 'rosusers:caps'
  | 'rosusers:error'
  | 'rosusers:ok'
  | 'router:active'
  | 'router:disabled'
  | 'router:status'
  | 'router:switched'
  | 'routers:update'
  | 'session:expired'
  | 'settings:pages'
  | 'setup:required'
  | 'stream:health'
  | 'wan:caps'
  | 'wan:error'
  | 'wan:ok'
  | 'wifiscan:done'
  | 'wifiscan:error'
  | 'wifiscan:interfaces'
  | 'wifiscan:rows'
  | 'wifiscan:state';
